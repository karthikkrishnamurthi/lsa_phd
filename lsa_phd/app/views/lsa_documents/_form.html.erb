<%= form_for(@lsa_document) do |f| %>
  <% if @lsa_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lsa_document.errors.count, "error") %> prohibited this lsa_document from being saved:</h2>

      <ul>
      <% @lsa_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :size => "60" %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :size => "120x25"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
